interface:
  display_name: "Daily News Picker"
  short_description: "인천교육청 언론보도 선별, 중복 제거, 근거 검증형 일일 브리핑"
  default_prompt: "Use $daily-news-picker to collect, read, deduplicate, rank, verify, and summarize recent news about the Incheon Metropolitan Office of Education and its affiliates. Treat user-provided reference links as monitored media sources and run the monitored outlet domain checks before concluding that coverage is sparse."
policy:
  allow_implicit_invocation: true
